# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Env / secrets
.env
backend/.env

# OS / editor
.DS_Store
Thumbs.db

# Build artifacts from doc tooling
unpacked_raw/
*.zip

# Sample/source data workbooks (contain PII — keep local only)
*.xlsx
# Real tournament CSV exports kept at the repo root for local testing
# carry real player names + USTA #s. The "Tournament *" prefix matches
# the file names the USTA dashboard exports.
Tournament *.csv
Tournament *.pdf
.archive/

# Claude scheduling runtime lock (transient)
.claude/scheduled_tasks.lock
