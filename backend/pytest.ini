[pytest]
testpaths = tests

# Coverage of shipped application code only (`app` = backend/app).
# Gate: python -m pytest --cov=app --cov-fail-under=100
[coverage:run]
source = app
relative_files = true
omit =

[coverage:report]
fail_under = 100
show_missing = true
exclude_lines =
    pragma: no cover
