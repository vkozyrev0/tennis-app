# Copy to backend/.env and adjust. POC defaults target localhost Postgres
# with the default admin user. Harden before any shared deployment
# (see docs/roadmap.md §Stack security note).
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=courtops
PGADMIN_DB=postgres
